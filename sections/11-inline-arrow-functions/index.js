// Creé una Arrow Function llamada showtext.
// Esta función retorna el texto "Hello World!".
const showtext = () => {
  return "Hello World!";
};

// Ejecuté la función y mostré el resultado en consola.
console.log(showtext());

// Creé una Arrow Function usando la forma corta.
// Como solo retorna un valor, no necesito usar {} ni return.
const showtext2 = () => "hades";

// Esta Arrow Function retorna un número.
const shownumber = () => 123;

// Esta Arrow Function retorna un valor booleano.
const showboolean = () => true;

// Esta Arrow Function retorna un arreglo.
const showarray = () => [1, 2, 3];

// Ejecuté cada función para ver qué retorna.
console.log(showtext2());
console.log(shownumber());
console.log(showboolean());
console.log(showarray());

// Lo que aprendí:
//
// Con llaves {} necesito usar return.
// const ejemplo = () => {
//   return "Hola";
// };
//
// Sin llaves {} el valor se retorna automáticamente.
// const ejemplo = () => "Hola";
//
// Una Arrow Function puede retornar:
// - Strings
// - Números
// - Booleanos
// - Arrays

// Creé una Arrow Function llamada showtext.
// Esta función retorna el texto "Hello World!".
const showtext = () => {
  return "Hello World!";
};

// Ejecuté la función y mostré el resultado en consola.
console.log(showtext());

// Creé una Arrow Function usando la forma corta.
// Como solo retorna un valor, no necesito usar {} ni return.
const showtext2 = () => "hades";

// Esta Arrow Function retorna un número.
const shownumber = () => 123;

// Esta Arrow Function retorna un valor booleano.
const showboolean = () => true;

// Esta Arrow Function retorna un arreglo.
const showarray = () => [1, 2, 3];

// Ejecuté cada función para ver qué retorna.
console.log(showtext2());
console.log(shownumber());
console.log(showboolean());
console.log(showarray());

// Lo que aprendí:
//
// Con llaves {} necesito usar return.
// const ejemplo = () => {
//   return "Hola";
// };
//
// Sin llaves {} el valor se retorna automáticamente.
// const ejemplo = () => "Hola";
//
// Una Arrow Function puede retornar:
// - Strings
// - Números
// - Booleanos
// - Arrays

// Nota para mí:
//
// Si tengo muchas funciones y no quiero escribir
// un console.log() para cada una, puedo ponerlas
// todas dentro de un solo console.log().
//
// Ejemplo:

console.log(showtext2(), shownumber(), showboolean(), showarray());

// Resultado:
// hades 123 true [1, 2, 3]
//
// Así ahorro líneas de código y puedo ver varios
// resultados con un solo console.log().

//   texto: showtext2(),
//   numero: shownumber(),
//   booleano: showboolean(),
//   arreglo: showarray(),
